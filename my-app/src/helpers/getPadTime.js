export const getPadTime = (time) => time.toString().padStart(2,0);//ставит 0 перед числом если оно не двухзначное
                        //Принимаемое //перевод    //заполнение строки,где 2 - требуемая длина
                        //значение    //в строку   // 0 - заполнение строки до заданной длины